# Project instructions

Read DATA_MODEL.md before data changes. Graph evidence must reference episode/time ranges;
never embed subtitle dialogue. Local text and work products belong in excluded local
storage. Preserve stable IDs, human decisions and coordinate sidecars. Do not generate substitute dialogue or annotations. Preserve existing Why content unless
the user explicitly requests changes.
Do not bump versions without explicit user instruction. Never perform visual verification;
use the non-visual tests and tools/check_public.py, and ask the user to review appearance.
